public = {
  index.html = index.ori()
  team = map(teamByName, =person.ori(@value), { extension: "->html" })
  assets
  images = drive/images
  thumbnails = map(images, =thumbnail(@value))
}

title = "Our Amazing Team"
teamByName = mapKeys(teamData.yaml, =name)

folderId = "1X3MWPXwwYXWarhNiCBIxCvGTyiBqISAF"
drive = cache(gdrive(folderId), cached)
teamData.yaml = headerKeys(parseJson(drive/Team))