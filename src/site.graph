public = {
  index.html = index.ori()
  team = map(teamByName, =person.ori(@value), { extension: "->html" })
  assets
  images = cached/images
  thumbnails = map(images, =thumbnail(@value))
}

title = "Our Amazing Team"
teamByName = mapKeys(teamData.yaml, =name)

folder = google(creds.json)/drive/1X3MWPXwwYXWarhNiCBIxCvGTyiBqISAF
cached = cacheSite(folder, gcache)
teamData.yaml = headerKeys(parseJson(cached/Team))